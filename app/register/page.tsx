'use client'
import { useState } from 'react'
export default function Register(){const[done,setDone]=useState('');async function submit(e:any){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/auth/register',{method:'POST',body:JSON.stringify(data)});setDone(r.ok?'Registered. Staff wait for principal approval. Parents can link child after login.':'Error registering')}return <main className="mx-auto max-w-xl p-6"><h1 className="text-3xl font-bold">Create account</h1><form onSubmit={submit} className="mt-6 space-y-3 rounded-3xl bg-white p-6 shadow"><input name="name" placeholder="Full name" className="w-full rounded-xl border p-3"/><input name="email" placeholder="Email" className="w-full rounded-xl border p-3"/><input name="password" placeholder="Password" type="password" className="w-full rounded-xl border p-3"/><select name="requestedRole" className="w-full rounded-xl border p-3"><option value="PARENT">Parent</option><option value="TEACHER">Teacher</option><option value="ADMIN">Admin</option></select><button className="w-full rounded-xl bg-violet-600 p-3 font-semibold text-white">Register</button>{done&&<p>{done}</p>}</form></main>}
